import 'package:flutter/material.dart';

class LoadingIndicatotrWidget extends StatelessWidget {
  const LoadingIndicatotrWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
